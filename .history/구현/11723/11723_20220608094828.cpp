#include <bits/stdc++.h>
using namespace std;

int main()
{
  freopen("input.txt", "r", stdin);

    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

	int m,num;
	string cmd;
	vector<int> s;

	cin>>m;

	for(int i=0;i<m;i++){
		cin>>cmd>>num;

		if(cmd == "add"){

		} else if (cmd == "remove"){

		}
	}


  
}