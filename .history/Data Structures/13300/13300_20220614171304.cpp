#include <bits/stdc++.h>
using namespace std;

int n,k;
vector<pair<int,int>> v;

int main()
{
  freopen("input.txt", "r", stdin); //제출 시 삭제

    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

  cin>>n>>k;

  for(int i=0;i<n;i++){
    int s, y;
    cin>>s>>y;

    v.push_back(make_pair(s,y));
  }

  for()



}