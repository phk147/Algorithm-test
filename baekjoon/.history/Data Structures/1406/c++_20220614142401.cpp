#include <bits/stdc++.h>
using namespace std;

list<char> li;

int main()
{
  freopen("input.txt", "r", stdin); //제출 시 삭제

    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

  string s;
  cin>>s;
  int len = s.length();

  for(char a : s) {
    li.push_back(a);
  }



}